{
  "name": "botshop-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix web && npm install --prefix admin-web && npm install --prefix worker",
    "dev:shop": "npm run dev --prefix web",
    "dev:admin": "npm run dev --prefix admin-web",
    "dev:worker": "npm run dev --prefix worker",
    "build:shop": "npm run build --prefix web",
    "build:admin": "npm run build --prefix admin-web",
    "build:all": "npm run build:shop && npm run build:admin",
    "deploy": "npm run build:all && npm run deploy --prefix worker"
  }
}
